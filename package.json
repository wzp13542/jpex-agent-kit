{
  "name": "jpex-agent-kit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Japanese-exchange-native AI trading toolkit — spot only, BitTrade first",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest",
    "lint": "eslint packages/",
    "format": "prettier --write 'packages/**/*.ts'",
    "typecheck": "tsc -b"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "eslint": "^9.0.0",
    "prettier": "^3.4.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  },
  "pnpm": {
    "overrides": {}
  }
}
